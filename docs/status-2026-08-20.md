# 施工状态快照（2026-08-20）

> 本文档是某一时刻的进度存档：未做任务 / 当前进度 / 遗留问题。
> 最新真相以 CHANGELOG（§13–§17）与 GitHub PR 为准；本文档随下一批施工归档或删除。

---

## 一、当前进度（全部已推送，待 review）

| 项 | 状态 |
|---|---|
| PR #2 `refactor/ai-package-v2` → main | 12 提交，**待 review 合并**（packages/ai v2 全量重构 + pi-ai 吸收两批） |
| PR #3 `feat/cache-write-and-catalog` → #2 | 1 提交，**待 review 合并**（cache_write 计费 + 目录导入 + CI；合并顺序：先 #2 后 #3） |
| 本地 `pnpm regress` | 31/31 全绿（typecheck/lint/build/test 全仓） |
| 覆盖率门禁 | ai 包 94.9/85.1/93.3/96.8、admin-api 93.8/85.2/94.5/95.2（阈值 90/85） |
| gateway e2e | 32/32 全绿（真服务 + dev 库 + 资金攻击面 + worker 全链） |
| DB 迁移 | 0062（providers.vendor）/ 0063+0064（cache_write 计费列）已应用本地 dev 库 |

### 已完成能力清单（自 2026-08-20 会话起）

1. **packages/ai v2 重构**（PR #2）：create-ai 768→273 行机制链拆解；五能力件契约 + defineAdapter 组合器；vendor 全链（providers.vendor → 档案库 → mergeParamRules → admin 下拉）；任务词表收敛 domain（删 ai 死模块 descriptors 133 行）
2. **修复 6 处生产缺陷**：①模态族寻址错路（endpoint 不透传）②multipart 文件丢失（upstreamForm 不拆包）③取消丢失（abort 事件不回放）④契约窄化 ⑤Anthropic 流式 usage 从未发出 ⑥Anthropic 输入口径（input_tokens 不含缓存 → 少收）
3. **pi-ai 资产吸收**：overflow 错误库（context_overflow 新码）、Mistral 方言、models.dev 元数据（1689 窗口 + 981 种子）、tiktoken 真分词器（主路径 + 启发式兜底）、vendor 档案 7 家、静默溢出兜底、cache_write 捕获
4. **cache_write 计费**（PR #3，系数体系）：三分段互斥公式、贵价上界、全链四处快照、admin 表单、真结算分毫断言
5. **目录导入**（PR #3）：审批制（草稿态 + 幂等 + dryRun + 单批 100）
6. **CI**（PR #3）：GitHub Actions 四门 + 双包覆盖率门禁 + PG/Redis 独享容器

---

## 二、未做任务（按优先级）

### P0 — 阻塞合并/上线

| # | 任务 | 说明 |
|---|---|---|
| 1 | **Review + 合并 PR #2 → PR #3** | 全部后续工作的前置；#3 堆叠在 #2 上 |
| 2 | **CI 首跑验证** | ci.yml 已入库但从未在 GitHub 上跑过——合并后首跑可能暴露环境差异（pnpm 版本/node 内存/迁移顺序），需盯首跑并修 |
| 3 | **生产 .env 补充** | cache_write 计费需运营在 admin 为 Claude 模型配置 cacheWritePrice（不配置=按输入价计，不会漏钱但也没赚差价）；导入种子需 curl 分批喂 |

### P1 — 短周期补齐（1-3 天/项）

| # | 任务 | 说明 |
|---|---|---|
| 4 | ~~静默溢出旗标接入告警~~ → **已交付（§19）** | ai 事件带 model → 网关订阅入 notify_outbox（dedupe 按请求）→ NOTIFY_EVENTS 词表 + UI 下拉加 context_overflow |
| 5 | ~~模型目录导入 admin 前端按钮~~ → **已交付（§18）** | 模型市场多源 Tab + 批量导入（预填换算内联编辑）+ models.dev 草稿流；裸种子 curl 通道已退役 |
| 6 | ~~models.dev 定期刷新~~ → **目录已活拉（§18）** | 市场页直拉 api.json（TTL 缓存）；仅 model-meta.generated.ts 再生成仍需手动跑脚本（编译期资产需 commit+build） |
| 7 | 真实上游凭证配置 | test/real 三场景（流式 usage/无效密钥分类/embeddings 寻址）代码就绪，本机无 MINIMAX_/DEEPSEEK_ key 自动 skip——配 key 才真跑 |

### P2 — 业务驱动（按需排期）

| # | 任务 | 说明 |
|---|---|---|
| 8 | 协议/供应商广度 | 7 协议 vs pi-ai 9 传输/35 provider；无 Midjourney/Suno/Realtime WS/可灵——新架构下新协议成本已降（codec + 组合件） |
| 9 | 多密钥渠道 | 渠道单 key，无 key 池轮询/按 key 禁用；等价做法是一 key 一渠道靠 weight 分摊 |
| 10 | 定时主动健康检查 | probe 仅管理端手动；自愈靠被动熔断 |
| 11 | vendor 档案继续扩充 | 7 家已入（pi-ai detectCompat 实证）；`tools[].function.strict` 嵌套删除现有 ignore 规则表达不了 |
| 12 | 供应商对账自动化 / 支付风控 / 代理商体系 / 企业 IdP / refresh_token / K8s 多副本 | requirements P2 项全部未动 |
| 13 | 网关 /metrics + Prometheus | 无 metrics 端点；观测栈 compose profile 默认关 |
| 14 | 资金包（wallet/service）覆盖率门禁 | 目前只有 admin-api 和 ai 包带阈值 |
| 15 | worker reconcile 哨兵测试 | 零测试（AGENT.md 已知未修项） |

---

## 三、遗留问题与风险（如实记录）

### 资金域

| # | 问题 | 状态 |
|---|---|---|
| R1 | **cache_write 对存量模型默认按输入价计** | 迁移默认 0 = 回落输入价（不漏钱）；但 Anthropic 实际成本 1.25×/2×——**未配置 cacheWritePrice 期间该分量平台贴钱**（收入=输入价 < 成本=1.25×输入价）。需运营尽快为 Claude 模型配价 |
| R2 | Anthropic 输入口径修复改变计费金额 | 修复后总输入 = 未缓存+读+写（旧=未缓存）——缓存命中量大且缓存价<输入价的存量配置，用户账单会**变大**（更准确了，但需告知） |
| R3 | 模型级估算校准仅 1 条 | tiktoken 主路径已大幅缓解；MiniMax-M3 之外的实测校准待补 |

### 工程域

| # | 问题 | 状态 |
|---|---|---|
| R4 | **共享 dev 库竞态**（本机活 worker） | e2e/pipeline 断言已加容忍（§5.7），但偶发抖动仍可能——根因是本机 `pnpm dev:all` 常驻；CI 独享库后此问题只在本地存在 |
| R5 | ~~CI 未验证~~ → **已跑绿（6m01s）** | 首跑连修 4 处后全绿：①迁移步改根四段链（原漏 identity-core/ledger-core provision，0055 回填依赖其建表）；②core env-secrets 测试直连 env 模块（整包 index 拖 otel/pg/redis 大图，慢机冷导入超 5s）；③ai BPE 两条测试 30s 超时（js-tiktoken 建 rank 表慢）；④rating-repo 自建 model_mappings（原依赖 dev 库种子残留）。附带：集成包 vitest 统一 15s 超时（对齐 wallet 先例） |
| R6 | 磁盘水位 | 本机根分区曾满（.turbo 13G 已清）；turbo 缓存会再涨，建议定期清或加清理任务 |
| R7 | 迁移纪律教训 | 曾修改已应用的 0063（已纠正为 0063+0064 分离）——流程教训记入 CHANGELOG §17 |

### 数据时效

| # | 问题 | 状态 |
|---|---|---|
| R8 | 模型元数据是静态快照 | models.dev 在线源本机不可达（网络），回落 pi-ai 本地快照生成；新模型发布后需手动重跑 `pnpm model-meta` |

---

## 四、合并后建议的验收顺序

1. 合 #2 → 跑 `pnpm regress` + `pnpm --filter @ai-gateway/ai test:coverage` 确认
2. 合 #3 → 盯 CI 首跑 → 修环境差异（如有）
3. 运营动作：为 Claude 系模型配置 cacheWritePrice（Anthropic 官方 5m=1.25×输入价、1h=2×）→ 观察首笔含写分量账单
4. curl 分批导入目录种子 → admin 复核上架若干模型 → 真实流量验证计费链路
